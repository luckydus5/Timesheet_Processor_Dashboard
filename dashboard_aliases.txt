# 🚀 TIMESHEET DASHBOARD ALIAS
# ============================
# Add this to your ~/.zshrc file for easy access

# Alias to run timesheet dashboard from anywhere
alias timesheet-dashboard="cd /home/luckdus/Desktop/Timesheet_Processor_Dashboard && export PATH=\"$HOME/.local/bin:$PATH\" && streamlit run timesheet_processor_dashboard.py --server.port 8505"

# Alternative shorter alias
alias tsd="cd /home/luckdus/Desktop/Timesheet_Processor_Dashboard && ./run_dashboard.sh"

# To add these aliases permanently:
# 1. Copy the lines above
# 2. Open your shell config: nano ~/.zshrc
# 3. Paste the lines at the end
# 4. Save and reload: source ~/.zshrc
# 5. Then you can use: timesheet-dashboard or tsd from anywhere